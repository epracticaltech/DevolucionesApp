# Base de datos
DB_URL=jdbc:postgresql://localhost:5432/devoluciones
DB_DATABASE=devoluciones
DB_USER=devoluciones
DB_PASSWORD=cambiar-password

# Seguridad JWT (Mínimo 32 caracteres / 256 bits)
JWT_SECRET=cambiar-por-secreto-de-al-menos-256-bits
JWT_EXPIRATION_MINUTES=60

# Carga Masiva
CARGA_DIR=../cargas
CARGA_MAX_FILAS=50000